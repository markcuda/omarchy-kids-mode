name: omarchy_kids_app
description: The parent app's Flutter UI over the omarchy_kids_parent logic package.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  omarchy_kids_parent:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
