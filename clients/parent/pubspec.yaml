name: omarchy_kids_parent
description: The parent app's signing, pairing and envelope code (N-8), proven against the box's shared vectors.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

dependencies:
  asn1lib: ^1.6.5
  cryptography: ^2.7.0

dev_dependencies:
  test: ^1.25.0
